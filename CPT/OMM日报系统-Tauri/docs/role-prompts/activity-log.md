# 三角色轻量工作记录

用途：记录三角色在各自职责范围内直接处理的低风险小事项，避免其他窗口不知道发生了什么。

规则：

- 只记已经完成的小事项，不写讨论过程。
- 每条一到两行，包含角色、事项、主要文件、验证结果。
- 不记录账号、PIN、个人数据、完整终端输出或大段实现细节。
- 影响版本、发布、数据库结构、清理策略、核心算法或用户数据的事项，不算小事项，必须进入正式交接或由杰写入 `CURRENT_STATUS.md`。
- 文件过长时由杰压缩为摘要，旧内容归档，不让它变成聊天记录仓库。

## 记录

- 2026-07-04 02:05，Codex：补充“小事自治与记录”规则，新增本文件；同步更新三角色提示词、文档治理和给杰交接。验证：已用 `rg` 核对入口路径与约束命中。
- 2026-07-04 02:05，Codex：补充“上下文恢复硬规则”和“不可丢失的协作记忆”；同步更新 `AGENTS.md`、`CURRENT_STATUS.md` 和当前给杰交接。验证：入口文档已指向恢复读取范围。
- 2026-07-04 02:05，Codex：新增 `core-memory.md` 作为深度记忆，摘要保存三角色、文档治理、小事自治、模块维护、安全、工具和发布规则；同步更新入口引用。验证：文件已加入新窗口默认恢复路径。
- 2026-07-04 02:12，Codex：新增给杰的初学者保护交接补充，覆盖决策分层、解释方式、三角色流程、完成定义和数据/文档护栏；同步更新入口和新窗口提示。验证：补充文件已列入恢复路径。
- 2026-07-04 02:25，寇：低风险核查中修复 `dev.bat` 开发模式标题乱码，并稳定 `useKnownSenderManager` 回调，避免送测人库弹窗反复刷新。验证：后续执行类型检查、Rust 检查和 Python 编译检查。
- 2026-07-04 02:25，寇：清理 `AGENTS.md` 和 `docs/new-codex-start-prompt.md` 三处 Markdown 行尾空格，避免 `git diff --check` 失败。验证：后续重跑 diff 检查。
- 2026-07-04，杰：补充凯的本地验收账号说明和“启动后直接进主页时忘记 PIN 暂不可测”的验收前提；主要文件：`docs/role-prompts/kai.md`、当前给杰交接。验证：未写入入口文档，未扩散 PIN 到活动记录。
- 2026-07-04 03:37，寇：发布前抢修送测人库锁库、旧备份清理确认和已知送测人无动作词识别；同时只读核查登录自动进主页来自有效 session。验证：后续执行类型检查、Rust 检查、Python 编译和识别样本检查。
- 2026-07-04 04:16，寇：跟进凯复验反馈，补旧备份清理按钮无反馈、访客账号补缺初始化、7.2B 已知送测人识别和送测人库保存反馈；O/C/手量及其量测人员仍作为非送测人处理。验证：后续执行类型检查、Rust 检查、Python 编译和定向解析检查。
- 2026-07-04 04:44，寇：按凯反馈修启动正式 Loading 文案、取消 session 自动进入主页，仅保留上次账户自动填入；补日期文件夹、设置中心和送测人库刷新中的禁用/旋转/结果反馈。验证：后续执行 TypeScript、Rust、Python 和 smoke 检查。
- 2026-07-04 05:08，寇：继续修凯的 dev 复验反馈，新增显式“记住我和 PIN”本机选项但仍需手动登录；统一静态首屏和 React Loading 为正式文案；刷新/读取状态增加最小可见时间、完成时间和补充规则表反馈。验证：后续执行 TypeScript、Rust、Python、smoke 和打包同步检查。
- 2026-07-04 05:24，寇：打 5.8.1 便携包时发现 PowerShell 5 默认编码会把 `tauri.conf.json` 中文读坏，导致 `package-portable.ps1` 解析失败；将配置读取改为 UTF-8。验证：后续重跑便携包脚本。
- 2026-07-04 05:39，寇：收紧 7.2B 已知送测人兜底规则，名字紧跟 `CMM/OMM/O/C/手量` 等量测标记时不再识别为送测人，避免把 CMM 侧人员当 sender。验证：后续重建 sidecar 并重跑 7.2B 定向解析。
- 2026-07-04，寇：按凯反馈优化预览页时长不足与排程警告展示；主提示/建议里的分钟格式统一为“约 N 分钟”，且有效时长不足时不再同时展示任务量过多/压缩类警告。验证：后续执行类型检查和 smoke。
- 2026-07-04，寇：修正排程顺序，隐形缓冲不再计入有效工时，也不再阻止手量/其他事务补时；7.2B 现先补入真实手量再追加少量缓冲。验证：Python 编译、7.2B 定向预览和 smoke 通过。
- 2026-07-04，寇：单日设置新增补时间手量/其他事务插入位置（头部/中部/尾部/随机），sidecar 预览和生成共用该策略，并在插入时给出排程提示。验证：TypeScript、Python、7.2B 位置策略定向预览和 smoke 通过。
- 2026-07-04，寇：修待生成队列右键菜单截断问题，新增队列卡片“设置”入口；同时将默认/智能下班策略收回正常班口径，只有显式下早班才走早班目标。验证：TypeScript、Python、7.2B 策略回归和 smoke 通过。
- 2026-07-04，寇：预览页新增生成前排程决策面板，策略/补时开关/补时上限/其他事务备注会更新当前日期并重新预览；其他事务备注可自定义并同步写入 Excel。验证：TypeScript、Python、exe 协议、Excel 临时输出和 smoke 通过。
- 2026-07-04，寇：收口生成前排程决策面板复验，移除缺时提示区重复生成按钮，保留统一“按当前结果生成”；重建 dev sidecar 后复核 7.2B 策略矩阵和其他事务备注回退/写入。验证：TypeScript、Python、exe 协议、smoke 和构建后 git status 通过。
- 2026-07-04，寇：确认 7.2B 手量/其他事务开启后仍不足属于当前 tpp/补时上限/可插入空档组合导致，非下早班策略回退；补充“补时仍不足”诊断文案，说明已插入量、上限、缺口和隐形缓冲不计有效。验证：Python、TypeScript、exe 协议、smoke 和构建后 git status 通过。
- 2026-07-04，寇：统一生成前决策顶部缺口文案格式，`decision.message` 和建议处理不再显示长小数，改为“约 N 分钟”。验证：Python、TypeScript、exe 协议、smoke 和构建后 git status 通过。
- 2026-07-04，寇：调整 7.2B 排程原则，普通料动态耗时先在允许范围内上调以满足有效工时，仍不足才插入手量/其他事务；预览细项补普通料均值、上限和可上调空间。验证：Python、TypeScript、exe 策略矩阵、smoke 和构建后 git status 通过。
- 2026-07-05，寇：修正 parser 机台号/测量员规则，OMM4#/OMM5#/CMM5# 不再被当成人，OMM4#-卫阳/OMM5#-卫阳 识别后续人名；新建文件夹和机台号缺人名目录进入审核，PCS 与 xlsx 件数不一致时默认采用文件夹 PCS 并给温和提示。验证：Python、TypeScript、dev sidecar exe 协议、smoke 和构建后 git status 通过。
- 2026-07-05，寇：修正焊接/AOI 品名口径，删除 41424-41429 -> 424-429 旧规则，焊接/AOI 样本优先取括号内 034~039；289/290 单独焊接样本保持自身品名。验证：Python、TypeScript、dev sidecar exe 协议、smoke 和构建后 git status 通过。
- 2026-07-05，寇：修正 xlsx 件数读取，EVT/全尺寸模板优先按短“检测工具”列锚定，统计 OMM 行下有效测量列，不再因 1-1/1-2/2-1 列头回退截断；文件夹 PCS 优先逻辑保持。验证：桌面 7.4B 样本读数 96、临时重复列头样本、parse_all_folders、dev sidecar exe 协议、smoke 和构建后 git status 通过。
- 2026-07-05，寇：新增测量人库，按普通 OMM 员工/班长管理/额外量测人员分角色种子，支持别名（魏泽元 -> 魏则元）并注入 sidecar 解析、预览和生成；设置中心新增测量人库维护入口。验证：Python、TypeScript、Rust cargo check、dev sidecar exe 协议、smoke 和构建后 git status 通过。
- 2026-07-05，寇：按凯复验反馈收紧测量人库口径，有启用测量人库注入时 unknown/停用别名不再通过 OMM/CMM 后缀中文名兜底识别 operator，改为进入审核；测量人库刷新按钮增加“已刷新 N 个启用条目”反馈。验证：Python、TypeScript、Rust cargo check、dev sidecar exe 协议、smoke 和构建后 git status 通过。
- 2026-07-05，寇：预览页新增“时间异常处理”第一阶段，summary 输出测料太少/任务量过多建议，前端顶部展示推荐调时或建议省略清单，帮助中心补说明；推荐只展示不移动文件夹。验证：Python、TypeScript、dev sidecar 构建、smoke 和构建后 git status 通过。
- 2026-07-05，寇：修正预览动作仍触发旧“每件时间偏低确认”的 P1 冲突，预览改由 time_anomaly 展示建议；生成前确认保留但普通料建议封顶 6 分钟/颗。验证：TypeScript、冲突文案 rg、smoke 和构建后 git status 通过。
- 2026-07-05，寇：完成下一阶段功能包：多日预览总览、测料太少推荐应用到本日包耗时覆盖、任务量过多省略清单确认后移动到新建文件夹A/B，并移除时间建议类旧弹窗。验证：TypeScript、Python、Rust、smoke 和构建后 git status 通过。
- 2026-07-05，寇：按凯复验反馈统一省略清单文案，明确默认只展示、通过二次确认后才移动到新建文件夹A/B；重建 dev sidecar 并打 5.8.1 便携包。验证：Python、TypeScript、smoke、Tauri build、便携包脚本和构建后 git status 通过。
- 2026-07-10，寇：修复日期文件夹小写 A/B 后缀识别，并更新白班休息时间为 11:50-12:50、16:40-17:20；A 班下早班也避开 16:40-17:20。验证：后续执行 Python、TypeScript、Rust、smoke 和打包检查。
- 2026-07-10，寇：生成前“保存设置并生成”会把预览页手量/其他事务补时草稿直接用于本次生成；smoke 增加 7.8A 禹欣两条识别回归，AGENTS 补换电脑部署检查。验证：后续执行 TypeScript、Python、smoke 和打包检查。
- 2026-07-10，寇：修正 xlsx 件数读取中短“检测工具”列后的右侧分析区边界，避免 CMM/判断区数据把 OMM 件数从 12 误算成 18；smoke 增加混合 OMM/CMM 合成回归。验证：后续执行真实 7.3B 样本、Python、smoke 和打包检查。
- 2026-07-10，寇：补送测时间紧贴送测人姓名的识别，并将工站冲突时 FAI 优先归为开发；版本同步升级到 5.8.2。验证：后续执行定向解析、TypeScript、Python、Rust、smoke 和打包检查。
- 2026-07-10，寇：恢复开屏等待文案为“少女祈祷中”，静态首屏与 React 登录加载页均使用全角“。。。”动画；版本同步升级到 5.8.3。验证：后续执行 TypeScript、Rust、smoke、Tauri build 和便携包检查。
- 2026-07-10，寇：修正同一 xlsx sheet 内纵向多联装测量表的件数统计，按每个短“检测工具”锚点分块统计 OMM 数据列并累加；默认打包流程改为只出便携版，安装包除非必要不构建；版本同步升级到 5.8.4。验证：后续执行真实 7.10A 样本、TypeScript、Python、Rust、smoke、sidecar 重建和便携包检查。
- 2026-07-10，寇：修复重复生成报表覆盖旧文件，改为自动追加（1）（2）后缀；省略清单移动增加预检和失败回滚，预览/单日设置按路径定位当前队列项。验证：Python、TypeScript、Rust、smoke 通过。
- 2026-07-10，寇：5.8.5 收口默认管理员初始 PIN 门禁、跨年报表日期推断和人员库注入失败可见报错；版本同步升级到 5.8.5。验证：后续执行 smoke、Tauri build、便携包和 git 推送检查。
- 2026-07-10，寇：清理本机旧 release，仅保留 5.8.5 便携版；补充换电脑部署检查清单并将 pillow 写入 sidecar requirements。验证：部署依赖 import 检查和文档 diff 检查通过。
